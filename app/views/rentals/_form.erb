<%@rental.errors.full_messages.each do |message|%>
 <b><%= message %><b></br>
<%end%>

<%= form_for @rental do |r|%>
  <%= r.label :start_date, "Data inicial"%>
  <%= r.date_field :start_date %></br></br>
  <%= r.label :end_date, "Data de término"%>
  <%= r.date_field :end_date %></br></br>
  <%= r.label :client_id, 'Cliente' %>
  <%= r.collection_select :client_id, @clients, :id, :name %>
  <%= r.label :car_category_id, 'Categoria' %>
  <%= r.collection_select :car_category_id, @car_categories, :id, :name %>
  <%= r.submit "Enviar" %>
<%end%>